{
  "name": "zc-wx-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev src/index.ts --remote=false",
    "deploy": "wrangler deploy src/index.ts",
    "d1:migrate": "wrangler d1 execute zc-db --local --file=./schema.sql",
    "d1:seed": "wrangler d1 execute zc-db --local --file=./seed.sql"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.28.1"
  }
}
